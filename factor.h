#ifndef FACTOR_H
#define FACTOR_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

void factor(char*);
void read_file(char *);
char **split_line(char *);

#endif /* FACTOR_H */
